/**
 * Public site config — safe to commit.
 * After deploying to Vercel, set tasksApi to your deployment URL.
 */
window.ALMANAC_CONFIG = {
  tasksApi: 'https://home-calendar-beta.vercel.app/api/tasks',
  requiresEditPassword: true,
};
